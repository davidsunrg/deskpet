export async function uploadFileWithPresignedUrl(_input: {
  file: File;
  uploadUrl: string;
  contentType: string;
}): Promise<void> {
  throw new Error('Pet avatar upload is not configured yet.');
}
